6.   vi.assem6.c

[참고] gcc 2.96 버전
- gcc 2.96 버전 이상 부터는 버퍼 8Byte 미만은 4Byte 할당
- gcc 2.96 버전 이상 버퍼 8Byte 이상부터는 16Byte 단위

함수가 입력되기전 push 인자값들이 중요하다. (볼때 이것위주로볼것.)

[참고] sticky 특수 퍼미션

- 디렉토리 안에 파일 생성은 가능하지만, 삭제가 불가능
- 단, 파일 소유자 및 Root는 삭제가 가능
