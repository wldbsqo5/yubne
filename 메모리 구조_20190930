
[낮은 주소]
      Text      실행 코드
      Data      sum1
                sum2
                sum3
      Heap      (동적으로 할당된 데이터)
        ↓

        ↑       (지역 변수, 함수 인자값, 리턴 주소)

                buf2         7byte
                buf1         7byte
      Stack     SFP(EBP)     4byte
                return       4byte    <- (악의적인 명령어가 있는 주소값 넣는곳)
[높은 주소]
